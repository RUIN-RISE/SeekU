# v1.1 Controlled-Open Closeout

Date: 2026-04-15
Stage: GitHub Expansion controlled-open
Owner: Codex
Status: closed

## Verdict

- current `v1.1 controlled-open` cycle: `closed`
- operational read: `done enough`
- discovery status: still paused by default

## Basis

This closeout is supported by the saved 2026-04-15 checkpoint and the shaped mainline batch:

- saved checkpoint: `.planning/github-expansion/WS4-CONTROLLED-OPEN-CHECKPOINT-2026-04-15.md`
- saved snapshot pack: `.planning/github-expansion/snapshots/ws4-rerun-2026-04-15-controlled-open-followup/`
- mainline batch note: `.planning/github-expansion/V1.1-BATCH-SHAPING-NOTE-2026-04-15.md`
- mainline repair note: `.planning/github-expansion/RETRIEVAL-REPAIR-FOLLOWUP-BATCH-2026-04-15.md`

Current saved judgment:

- `Q4`: `watch-but-stable`
- `Q6`: `pass`
- `Q8`: `pass`
- `controlled-open sustain` gate: `clear`

Current code-path validation:

- `pnpm exec vitest run packages/search/src/__tests__/source-filter.test.ts packages/search/src/__tests__/retriever-fallback.test.ts packages/search/src/__tests__/reranker-open-source.test.ts apps/worker/src/search-cli.test.ts`
- `pnpm --filter @seeku/search typecheck`
- `pnpm --filter @seeku/worker typecheck`

Observed result at closeout time:

- `22` tests across `4` files passed
- both package typechecks passed
- no new evidence justified another mainline repair loop

## What This Closure Means

This closeout means:

- the current `v1.1 controlled-open` cycle has reached a stable stopping point
- the mainline search patch is reviewable as a bounded search-quality batch
- routing is now allowed to move from search repair into workspace cleanup

This closeout does **not** mean:

- `Q4` is fully closed; it remains an explicit `watch-but-stable` family
- discovery should restart by default
- Bonjour sidecar tooling becomes part of the default `v1.1` mainline
- workstream migration becomes the active planning source of truth

## Default Next Move

Clean the current workspace.

Use the shaped split from `.planning/github-expansion/V1.1-BATCH-SHAPING-NOTE-2026-04-15.md`:

1. mainline search batch
2. docs cleanup batch
3. sidecar Bonjour batch
4. verification helper batch

Operational rule:

- do not mix Bonjour sidecar files into the mainline review
- do not reopen planner/retriever/reranker scope unless a new saved checkpoint regresses
- if a future `Q4` follow-up is required, prefer narrow late-ranking work first

## Short Decision

`v1.1 controlled-open` is closed for this cycle. The correct next step is workspace cleanup and batch landing, not another retrieval or discovery push.
