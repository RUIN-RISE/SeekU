# Phase 05.3: Data Quality Repair - Execution Plan

**Phase:** 05.3-data-quality-repair
**Status:** ✅ COMPLETED
**Completed:** 2026-03-30
**Executor:** Codex (Execution) + Claude Code (Review)

---

## Problem Analysis

| Field | Current Fill Rate | Root Cause |
|-------|-------------------|------------|
| headline | 3.2% | Merger only lifts `profile.headline`, ignores `bio/summary` (90% filled) |
| facetSource | 3.2% | Only extracts from evidence (3.2% coverage); payload stringified breaks extraction |
| facetRole | 3.2% | Extractor only reads `primaryHeadline` + `job_signal`, ignores `summary/bio` |

**Key Insight:** Not a data source problem. It's a pipeline/derivation problem.

---

## Execution Order

```
P0 (Pipeline) → P1 (Headline) → P2 (facetRole) → Rebuild Search Docs
```

---

## P0: Evidence Pipeline Repair

### Tasks

| # | Task | File | Line |
|---|------|------|------|
| P0-1 | Add `coerceJsonObject` helper to parse stringified JSON | `packages/db/src/repositories.ts` | L92 |
| P0-2 | Apply coercion in `upsertSourceProfile` | `packages/db/src/repositories.ts` | L145 |
| P0-3 | Apply coercion in `profileToUpsertPayload` | `packages/db/src/repositories.ts` | L567 |
| P0-4 | Coerce payload before `extractAllBonjourEvidence` | `packages/workers/src/evidence-storage.ts` | L46 |
| P0-5 | Coerce in `getNormalizedProfile` | `packages/identity/src/merger.ts` | L17 |
| P0-6 | Add identity/source-profile fallback for `facetSource` | `packages/search/src/index-builder.ts` | L28 |
| P0-7 | Create `repair-source-profiles.ts` worker | NEW: `packages/workers/src/` | - |
| P0-8 | Export repair worker | `packages/workers/src/index.ts` | L1 |
| P0-9 | Add `repair-source-payloads` CLI command | `apps/worker/src/cli.ts` | L12 |

### Verification

```bash
pnpm --filter @seeku/worker build
pnpm --filter @seeku/worker start repair-source-payloads
pnpm --filter @seeku/worker start store-evidence
pnpm --filter @seeku/worker start rebuild-search
```

```sql
-- Expect object type, not string
SELECT source, jsonb_typeof(raw_payload), jsonb_typeof(normalized_payload), COUNT(*)
FROM source_profiles GROUP BY 1,2,3;

-- Expect > 1
SELECT COUNT(DISTINCT person_id) FROM evidence_items;
```

---

## P1: Headline Backfill

### Tasks

| # | Task | File | Line |
|---|------|------|------|
| P1-1 | Add `buildHeadline(profile)` with priority fallback | `packages/adapters/src/bonjour/normalize.ts` | L117 |
| P1-2 | Modify `selectPrimaryHeadline` to use fallback | `packages/identity/src/merger.ts` | L41 |
| P1-3 | Apply new headline logic in `attachProfilesToPerson` | `packages/identity/src/merger.ts` | L129 |
| P1-4 | Create `backfill-person-fields.ts` worker | NEW: `packages/workers/src/` | - |
| P1-5 | Export backfill worker | `packages/workers/src/index.ts` | L1 |
| P1-6 | Add `backfill-person-fields` CLI command | `apps/worker/src/cli.ts` | L68 |

### Priority Fallback Logic

```
basicInfo.role > current_doing > first line of bio > first line of summary
```

### Verification

```bash
pnpm --filter @seeku/worker build
pnpm --filter @seeku/worker start backfill-person-fields
pnpm --filter @seeku/worker start rebuild-search
```

```sql
-- Target: > 80%
SELECT
  COUNT(*) FILTER (WHERE primary_headline IS NOT NULL) * 100.0 / COUNT(*) AS fill_rate
FROM persons WHERE search_status = 'active';
```

---

## P2: facetRole Extraction Enhancement

### Tasks

| # | Task | File | Line |
|---|------|------|------|
| P2-1 | Expand `extractRoles` to read `summary/bio/profile_field` | `packages/search/src/index-builder.ts` | L49 |
| P2-2 | Add role signal metadata extraction | `packages/identity/src/evidence/bonjour.ts` | L91 |
| P2-3 | Add tests for Chinese/English role extraction | NEW: `packages/search/src/__tests__/index-builder.test.ts` | - |

### Verification

```bash
pnpm test -- packages/search/src/__tests__/index-builder.test.ts
pnpm --filter @seeku/worker start rebuild-search
```

```sql
-- Target: > 50%
SELECT
  COUNT(*) FILTER (WHERE cardinality(facet_role) > 0) * 100.0 / COUNT(*) AS fill_rate
FROM search_documents sd
JOIN persons p ON p.id = sd.person_id
WHERE p.search_status = 'active';
```

---

## Final Rebuild

After all P0/P1/P2 fixes:

```bash
pnpm --filter @seeku/worker start rebuild-search
pnpm --filter @seeku/worker start search "杭州"
```

---

## Success Criteria

| Metric | Before | After | Target | Status |
|--------|--------|-------|--------|--------|
| evidence coverage | 3.2% | 93.5% | > 50% | ✅ PASS |
| headline fill rate | 3.2% | 100% | > 80% | ✅ PASS |
| facetRole fill rate | 3.2% | 87.1% | > 50% | ✅ PASS |
| facetSource fill rate | 3.2% | 100% | > 90% | ✅ PASS |

---

*Plan created: 2026-03-30*
*Completed: 2026-03-30*