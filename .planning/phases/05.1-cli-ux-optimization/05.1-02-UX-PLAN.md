# 05.1-02: UX 交互优化 (ora spinner)

## Goal
Improve perceived performance and user feedback during long-running LLM operations by integrating `ora` spinners.

## Proposed Changes
### [DEPENDENCY] [package.json](file:///Users/rosscai/seeku/apps/worker/package.json)
- Add `ora@8.0.1`.

### [MODIFY] [workflow.ts](file:///Users/rosscai/seeku/apps/worker/src/cli/workflow.ts)
- Initialize/Stop spinner in `execute()`:
  - "Extracting requirements..."
  - "Searching for candidates..."
- Initialize/Stop spinner in `showCandidateDetail()`:
  - "Analyzing profile..."
  - "Generating deep insights..."
- **CRITICAL**: Ensure spinner is stopped before `tui.selectCandidate()` and `readline.question()`.

### [MODIFY] [chat.ts](file:///Users/rosscai/seeku/apps/worker/src/cli/chat.ts)
- Add "Refining search intent..." spinner overlay during `refineConditions`.

## Verification
- Run search; observe spinner behavior.
- Run candidate detail; observe spinner behavior.
- **Conflict Check**: Verify no flickering or stdout corruption when enquirer prompt appears.
