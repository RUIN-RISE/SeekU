---
context: cli-upgrade
phase: command-surface-hardening-and-raw-mode-decision
task: batch-closeout
status: ready_for_review
last_updated: 2026-04-24T09:20:00+08:00
---

# Continue Here — CLI Command Surface Batch

## Current Batch

A coherent review batch is staged for the CLI command surface hardening work:

- `/memory` works in shortlist, compare, detail, and launcher contexts.
- Shortlist slash aliases are consistent: `/m` opens memory, bare `m` still shows more candidates.
- Global commands exposed by the command palette now have behavior in active views:
  - `/task` and `/workboard` show the current workboard overlay.
  - `/transcript` shows the current transcript.
  - `/new` and `/tasks` leave the current workflow and return to launcher flow.
- Clarify freeform prompts intercept slash commands at workflow level instead of treating them as natural language.
- Phase 6 raw-mode evaluation is documented and tracked at `.planning/PHASE_6_RAW_MODE_EVALUATION.md`.

## Verification

Latest verification run: 2026-04-24.

- `pnpm --filter @seeku/worker typecheck` — pass.
- CLI-focused Vitest suite — pass: 9 files, 209 tests.
  - `command-spec.test.ts`
  - `command-router.test.ts`
  - `shell-renderer.test.ts`
  - `launcher-v2.test.ts`
  - `command-palette.test.ts`
  - `tui.test.ts`
  - `index.test.ts`
  - `workflow.test.ts`
  - `workflow-session-events.test.ts`

Known non-failing output:

- Vitest prints `environmentMatchGlobs` deprecation.
- Workflow session tests print existing `[Pool Warning]` messages.

## Next Move

1. Review or commit the staged CLI command surface batch.
2. Leave `docs/superpowers/plans/2026-04-22-agent-product-memory-task-model-implementation-plan.md` out of this batch unless it is explicitly approved for inclusion.
3. After this batch is closed, continue to Phase 8 from `.planning/CLI_UPGRADE_IMPLEMENTATION_PLAN.md`: mascot + visual polish.

## Notes

- Phase 6 decision: skip full raw-mode migration.
- Phase 7 remains skipped because raw-mode migration is not recommended.
- Current GSD roadmap still says milestone v1.8 Phase 14 is active; the CLI upgrade Phase 8 reference is from `.planning/CLI_UPGRADE_IMPLEMENTATION_PLAN.md`, not the main roadmap phase number.
