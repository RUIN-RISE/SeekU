# Requirements Archive — Seeku v1.5

Archived: 2026-04-18
Milestone: `v1.5 Chat-First Copilot`
Status: Complete

## Archived Requirements

- [x] `COPILOT-01` — `/chat` becomes Seeku's default primary operator surface for the next milestone instead of treating `/deal-flow` or `/agent-panel/[sessionId]` as parallel main entry points.
- [x] `COPILOT-02` — The `/chat` surface renders a split `chat + right rail` layout where chat remains the only primary control thread.
- [x] `COPILOT-03` — The right rail is a read-only `Narrated Workboard` with fixed sections: `Now`, `Why`, `Movement`, and `Focus`.
- [x] `COPILOT-04` — The workboard translates the authoritative runtime modes `clarify`, `search`, `narrow`, `compare`, and `decide` into stable human-readable narration instead of exposing a raw event timeline.
- [x] `COPILOT-05` — The workboard consumes the existing authoritative session snapshot and event stream rather than inventing a second browser-owned business-state model.
- [x] `COPILOT-06` — The `Focus` section can render current-session outputs including goal summary, shortlist summary, compare summary, and `Top 3 today` / recommendation posture without making deal flow a separate runtime mode.
- [x] `COPILOT-07` — The right rail remains observation-only in v1.5; steering and state changes continue to happen through natural-language chat rather than direct workboard controls.
- [x] `COPILOT-08` — Persistent signals such as drift and uncertainty may appear only when they materially affect the current session and must not turn the surface into a CRM or memory console.
- [x] `COPILOT-09` — The product degrades safely for no-live-session, partial snapshot, and event disconnect states without fabricating progress or stale candidate state.
- [x] `COPILOT-10` — Transitional `/agent-panel/[sessionId]` and `/deal-flow` routes remain compatible during rollout, but product behavior progressively reuses the new chat-first workboard rendering logic.
- [x] `COPILOT-11` — The milestone preserves CLI runtime authority, compare gating, recommendation honesty, and saved search-quality posture including `Q4` `watch-but-stable`, `Q6` `pass`, and `Q8` `pass`.
- [x] `MISSION-01` — The chat-first copilot can start one foreground `large-scope candidate search` mission inside a session without creating a second runtime authority.
- [x] `MISSION-02` — A mission runs through bounded phases such as `running_search`, `narrowing`, `comparing`, `summarizing`, and `stopped` rather than an unbounded free-form loop.
- [x] `MISSION-03` — The mission stops automatically with an explicit stop reason chosen from `enough_shortlist`, `enough_compare`, `low_marginal_gain`, or `needs_user_clarification`.
- [x] `MISSION-04` — The user may interrupt a running mission with natural-language course corrections, and those corrections stay inside the same mission instead of spawning a new task.
- [x] `MISSION-05` — The chat-first UI shows a mission frame, active phase, latest movement, and final stop outcome without turning the interface into a background task center or a raw event log.
- [x] `MISSION-06` — The first mission runner supports only one active mission per session and remains foreground-bound; it does not continue after the user leaves.
- [x] `MISSION-07` — Mission execution preserves compare gating, recommendation honesty, and the saved `Q4/Q6/Q8` quality posture.

## Outcome Notes

- `COPILOT-01` to `COPILOT-05`: validated through Phase 10 shell, binding, and workboard delivery
- `COPILOT-06` to `COPILOT-10`: validated through Phase 10 integration of `Focus`, transitional routes, and fallback handling
- `COPILOT-11`: validated through carry-forward quality guardrails and milestone regression coverage
- `MISSION-01` to `MISSION-04`: validated through Phase 11 mission object, loop, and correction handling
- `MISSION-05` to `MISSION-07`: validated through Phase 11 stop-quality acceptance and mission-quality regression coverage

## Final Traceability

| Requirement | Final status | Evidence |
|-------------|--------------|----------|
| `COPILOT-01` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/app/page.tsx` |
| `COPILOT-02` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/components/ChatInterface.tsx` |
| `COPILOT-03` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/components/ChatCopilotWorkboard.tsx` |
| `COPILOT-04` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/components/ChatCopilotWorkboard.tsx` |
| `COPILOT-05` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/hooks/useChatSession.ts` |
| `COPILOT-06` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/components/ChatCopilotWorkboard.tsx` |
| `COPILOT-07` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/components/ChatCopilotWorkboard.tsx` |
| `COPILOT-08` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/components/ChatCopilotWorkboard.tsx` |
| `COPILOT-09` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/components/__tests__/ChatCopilotWorkboard.test.ts` |
| `COPILOT-10` | Complete | `.planning/phases/10-chat-first-copilot/SUMMARY.md`, `apps/web/src/app/agent-panel/[sessionId]/page.tsx`, `apps/web/src/components/DealFlowBoard.tsx` |
| `COPILOT-11` | Complete | `.planning/milestones/v1.5-ROADMAP.md`, `.planning/phases/11-bounded-mission-runner/ACCEPTANCE-2026-04-18.md` |
| `MISSION-01` | Complete | `.planning/phases/11-bounded-mission-runner/SUMMARY.md`, `apps/web/src/hooks/useChatSession.ts` |
| `MISSION-02` | Complete | `.planning/phases/11-bounded-mission-runner/SUMMARY.md`, `apps/web/src/hooks/useChatSession.ts` |
| `MISSION-03` | Complete | `.planning/phases/11-bounded-mission-runner/SUMMARY.md`, `apps/web/src/hooks/mission-stop-policy.ts` |
| `MISSION-04` | Complete | `.planning/phases/11-bounded-mission-runner/SUMMARY.md`, `apps/web/src/hooks/__tests__/useChatSession.test.ts` |
| `MISSION-05` | Complete | `.planning/phases/11-bounded-mission-runner/SUMMARY.md`, `apps/web/src/components/ChatInterface.tsx`, `apps/web/src/components/ChatCopilotWorkboard.tsx` |
| `MISSION-06` | Complete | `.planning/phases/11-bounded-mission-runner/SUMMARY.md`, `apps/web/src/hooks/useChatSession.ts` |
| `MISSION-07` | Complete | `.planning/phases/11-bounded-mission-runner/ACCEPTANCE-2026-04-18.md`, `apps/web/src/hooks/__tests__/useChatSession.test.ts` |

## Carry-forward Notes

- This archive is milestone-scoped. The next milestone should open with a fresh active `REQUIREMENTS.md`.
- No background continuation, multi-mission management, CRM workflow, or durable task history was added in v1.5.
- Mission-quality acceptance is currently based on deterministic replay-style scenarios rather than live search-distribution replays.
